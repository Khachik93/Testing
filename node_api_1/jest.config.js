const config = {
    verbose: true,
    moduleFileExtensions: [
        "js",
        "json",
      ],
      automock: false,
      reporters: undefined,
      testURL: "http://localhost",
      transformIgnorePatterns: [
        "/node_modules/"
      ],
  };
  
  module.exports = config;